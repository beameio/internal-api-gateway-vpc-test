# internal-api-gateway-vpc-test

Cloud Formation example of an internal API gateway + lambda (inside internal VPC) that is accessed from an external facing API Gateway + lambda (Experience API style) 
